import 'package:flutter/material.dart';

class NewFeedsPage extends StatelessWidget{
  const NewFeedsPage({Key?key}):super(key: key);

  @override
  Widget build(BuildContext context){
    return Scaffold(
body: const Center(
  child: Text('NewFeedsPage'),
)
    );
  }
}